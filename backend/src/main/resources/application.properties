# ── MongoDB ──
spring.data.mongodb.uri=mongodb+srv://samriddhi324_db_user:track123@m0cluster.brogyyk.mongodb.net/jobtracker?retryWrites=true&w=majority
spring.data.mongodb.auto-index-creation=true

# ── Server ──
server.port=8080

# ── JWT ──
app.jwt.secret=CHANGE_ME_to_a_base64_encoded_256bit_secret_key_in_production
app.jwt.expiration-ms=86400000

# ── CORS ──
app.cors.allowed-origins=http://localhost:4200

# ── Logging ──
logging.level.com.tracker=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
